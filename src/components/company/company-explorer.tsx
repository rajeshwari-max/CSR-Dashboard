"use client";

/**
 * Kept as a stable import path — the standalone explorer was folded into the
 * fuller Company Analysis page (search + comparison + disclosure documents).
 */
export { CompanyAnalysisView as CompanyExplorer } from "@/components/pages/company-analysis-view";
